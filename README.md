using System;

class MainClass {
    static void Main(string [] args) {
       int v, m ,n;
       
        v = Convert.ToInt32 (Console.Readline ());
        m = Convert.ToInt32 (Console.Readline ());
        Console.WriteLine(m);
        Console.Readline ();
    }
}
